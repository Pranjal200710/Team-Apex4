const caregiverId='C001', container=document.querySelector('#requests');
const label=a=>a.replaceAll('_',' ').toLowerCase().replace(/\b\w/g,c=>c.toUpperCase());
async function decide(id,decision){await fetch(`/api/security/requests/${id}/${decision}`,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({caregiver_id:caregiverId})});load()}
async function load(){try{const r=await fetch(`/api/security/requests/${caregiverId}`);const data=await r.json();if(!data.length){container.innerHTML='<div class="empty">No approval requests yet.</div>';return}container.innerHTML=data.slice().reverse().map(x=>`<article class="request"><div><span class="badge ${x.risk_level==='CRITICAL'?'critical':''}">${x.status} · ${x.risk_level}</span><h2>${label(x.action)}</h2><p><b>Patient:</b> ${x.patient_name}</p><p><b>Resource:</b> ${x.resource}</p>${x.amount?`<p><b>Amount:</b> ₹${x.amount} · ${x.purpose||'Healthcare payment'}</p>`:''}<p><b>Reason:</b> ${x.reason}</p><p><b>Result:</b> ${x.result||'Awaiting caregiver decision'}</p></div>${x.status==='PENDING'?`<div class="actions"><button class="approve" onclick="decide('${x.id}','approve')">YES, APPROVE</button><button class="deny" onclick="decide('${x.id}','deny')">NO, DENY</button></div>`:''}</article>`).join('')}catch{container.innerHTML='<div class="empty">Backend unavailable. Start the FastAPI server first.</div>'}}
load();setInterval(load,3000);
